# Strength Execution Flow

Stage 6A: the execution flow for `.strength`/`.hypertrophy`/`.accessory`
`WorkoutBlock`s — one foundation shared by Hypertrophy and Powerlifting
(§13), since both already produce the identical `ExercisePrescription`/
`SetPrescription`/`SetResult` shape. **Design pass — nothing here is
implemented yet.**

## 1. Screen anatomy (per `Training OS.dc.html` frames 03/07, "live" fidelity)

One exercise (`ExercisePrescription`) on screen at a time, its
`orderedSetPrescriptions` driving the set list:

```
‹  Upper Push A            Block 2 of 4 · Strength      11:02
[███████░░░░]  (per-block progress strip, one segment per block)

Back Squat
4 × 5–6 @ 2 RIR                                    Substitute →

┌─ Suggested ──────────────┬─ Last time · 6 d ago ──────────┐
│ 115 kg                   │ 115 × 6 · 115 × 6 · 115 × 5     │  Why?
└───────────────────────────┴──────────────────────────────────┘

Set 1   115 × 6   RIR 2   ✓        (logged — collapsed row)
Set 2   115 × 6   RIR 1   ✓        (logged — collapsed row)
┌─ Set 3 · logging ─────────────────────────────────────────┐
│  kg          reps          RIR         [ done ]            │
│ − 115 +     − 6 +      [0][1][2✓][3][4]                    │
└──────────────────────────────────────────────────────────────┘
Set 4   115 × 5–6  RIR 2   (not yet — dashed outline)

+ Add set                                              Notes

[            Log set 3 · rest 3:00            ]
```

Every element above already maps to an existing typed field — nothing
in this layout requires new domain schema beyond what §8/§12 name.

## 2. Suggested load (§8 of the kickoff)

Shown once per exercise, above the set list, **before** any set is
logged — derived from `SetPrescription` + `PerformanceProfile` +
`ProgressionEngine`, never overwriting the prescription:

```
suggested = ProgressionEngine.recommend(
    ProgressionInput(
        targets: exercisePrescription.orderedSetPrescriptions.map { SetTarget(repRangeLow: $0.repRangeLow, repRangeHigh: $0.repRangeHigh, targetRir: $0.targetRir) },
        latestResults: <last session's SetOutcome[] for this ExercisePerformanceProfile>,
        hasUsableHistory: exerciseProfile.confidence > 0 (or a configured threshold — see PROGRAM_RECOMMENDATION_MODEL.md's own confidence-threshold precedent),
        equipmentIncrement: user's EquipmentProfile.smallestIncrementKg for this exercise's equipment,
        lastKnownWeight: exerciseProfile's most recent SetResult.weight
    )
)
```

- **Display:** suggested load (large), target reps (`repRangeLow`–`repRangeHigh`
  from `SetPrescription`, unchanged), target RIR, and — tappable — a
  "Why?" affordance opening the reason sheet.
- **Why sheet** (frame 03's live overlay): plain-language headline, then
  a structured key/value block — `Reason code` (`ProgressionReasonCode`,
  the exact stable identifier, never hidden), `Prescription`, `Previous`,
  `Increment`, `Confidence` (+ age). Generated once, cached on a
  `Recommendation` row (`Recommendation.inputsSummary`), never
  regenerated by re-running the engine every time the sheet opens —
  matching `Recommendation.swift`'s own "so it can back a Why sheet
  without re-running the engine" doc comment.
- **Never overwrites `SetPrescription`.** The suggested value is
  read-only engine output; the *target* the user actually sees per set
  (§3) still comes from `SetPrescription.targetWeight`/`repRange*`/
  `targetRir`, materialized ahead of time. If a caller wants the
  suggestion to become the day's real target, that is a decision made
  once, before the Session materializes (the existing generator/
  materializer path) — never a live rewrite mid-session.
- **History context** (§30): "Last time" shows the exercise's most
  recent completed session's sets (`ExercisePerformanceProfile.orderedSetResults`,
  most recent few only — 2-3 sets, never a full history dump on this
  screen; full history lives on the Exercise detail screen, Progress
  tab, out of this stage's scope but already schema-supported).

## 3. Per-set logging (§5-6 of the kickoff)

Each `SetPrescription` pre-fills its row: `targetWeight` (rounded via
the user's `EquipmentProfile.resolve(_:)`, never shown unrounded),
`repRangeLow`–`repRangeHigh` (or a single suggested value, TRAININGOS_DESIGNED
choice — see decision memo), `targetRir` **preselected** among the RIR
chips. Logging one set is:

```
weight  ±stepper, increment = EquipmentProfile.smallestIncrementKg
reps    ±stepper
RIR     row of chips (0…4, or whatever range the program configures —
        preselected at targetRir; tapping a different chip is the only
        extra interaction beyond confirming)
[done]  → RecordSetResultUseCase.recordSet(... actualRir: <chip value> ...)
```

Confirming with the preselected RIR is **one tap** — the button always
reads `"Log set N · rest M:SS"` (the rest-timer default folded directly
into the CTA label, so starting rest requires no separate action, per §7).
Long-press a stepper for keypad entry (design source, frame 03's "Long-press
a value for keypad entry" spec) — a UI affordance, no new domain
requirement.

`actualRir` is **always persisted per set**, whether it matched the
target or not — `SetResult.actualRir` already exists exactly for this;
there is no "skip logging RIR" path, matching the Handoff's locked
"RIR: engine owns target, user owns actual, stored per set, preselected
at target" decision.

**Completed/skipped:** a set can be marked skipped instead of logged
(the toggle button in frame 03's row, `{{ s.toggle }}`) — this simply
means no `SetResult` is created for that `SetPrescription`; the
prescription itself is never deleted or mutated. A block can still reach
`.completed` with fewer `SetResult`s than `SetPrescription`s — exactly
`SESSION_STATE_MACHINE.md` §5's "block-level partial-ness needs no new
enum case."

## 4. RIR interaction (§6) — no new screens

The chip row **is** the entire RIR interaction — no secondary
confirmation screen, no separate "how did that feel" survey. Per the
design source's own locked language: *"RIR arrives preselected at the
prescribed target; confirming logs the set in one tap... Tapping a
different RIR chip is the only extra interaction, and `actualRir` is
persisted per set regardless."* This stage does not introduce anything
beyond what's already approved — the chip range (typically 0-4) and its
visual treatment are UI polish, not new business logic.

## 5. Rest timer (§7)

A `TimerState` (`TIMER_ARCHITECTURE.md` §2/§3) starts the instant a set
is logged, if the rest timer is enabled (a per-user or per-program
setting — TRAININGOS_DESIGNED default, see decision memo):

- **Configurable default** — a plain duration (e.g. the program's own
  configured rest, or a user default), never derived from progression
  logic.
- Runs visibly (the CTA/header shows the countdown) but **non-blocking**
  — the user may log the next set's stepper adjustments, open Notes, or
  substitute the exercise while it counts down; nothing about set
  logging is gated on the rest timer completing.
- **Skip/adjust**: tapping the rest indicator lets the user skip
  straight to the next set or add/remove time — a plain `TimerState`
  mutation (new `startedAt`/`targetDurationSeconds`), never touching
  `SetResult`/`Recommendation`.
- A local notification fires when rest completes if the app is
  backgrounded (`TIMER_ARCHITECTURE.md` §6) — presentation only.
- **Never coupled to `ProgressionEngine`** — per the kickoff's explicit
  instruction, rest duration is never computed *from* a progression
  reason code (e.g. "longer rest because load increased"); if a program
  wants variable rest, that is a plain configured value on the
  prescription, never a live engine call.

## 6. Mid-session / within-session recommendation (§9) — none exists; document, don't invent

**Finding, confirmed by reading every existing engine:**
`DoubleProgressionEngine`/`StrengthProgressionEngine` both operate at
the granularity of *"last session's whole set of results → this
session's target"* — never *"set 1 of today → adjust set 2 of today."*
`StrengthProgressionEngine.resolveSetCount`'s `.autoregulated` case
takes a live `autoregulationRating`, but per its own doc comment that
rating is sourced from *"the paired slot's logged feedback for that
week"* (a cross-slot, week-boundary input resolved once when the next
week's `SetPrescription`s are materialized) — not a same-session,
same-exercise, set-to-set live adjustment.

**Resolution: Stage 6 does not add mid-session load/rep adjustment.**
Every `SetPrescription` a Session already carries is shown exactly as
materialized, set after set, regardless of how set 1 went. This is not a
missing feature to build around — it is the correct, current boundary of
what exists, and the kickoff's own instruction ("do not invent new
autoregulation rules in UI. Use existing engine contracts only") is
satisfied by leaving this alone. If genuine intra-session autoregulation
is wanted later, it needs a new, explicitly-designed engine contract —
out of scope here, flagged in `STAGE6A_DECISION_MEMO.md` §7.

## 7. Exercise substitution (§10-11, Stage 4C reused exactly)

From the exercise title: **"Substitute"** →

```
SubstitutionValidator-filtered candidates, ranked deterministically by
ExerciseRelationshipResolver.bestEstimateSource (curated .directSubstitute
beats .similarMovement beats shared-target/pattern/equipment matches) —
never an LLM, never a free-text search that could resolve to a
non-canonical string.
```

Presenting the choice: **"Today only"** vs. **"Going forward"** — the
exact two Stage 4C scopes, unchanged:

- **Today only** → `SubstituteExerciseUseCase.substituteThisSessionOnly`
  — edits the already-materialized `ExercisePrescription.exercise`
  directly; `ProgramInstance`/future materialization/`ProgramDefinition`
  untouched.
- **Going forward** → `SubstituteExerciseUseCase.substituteGoingForward`
  — writes/updates a `SlotSelectionOverride` on the `ProgramInstance`;
  future materialization reads it via
  `SubstituteExerciseUseCase.resolvedExercise`; **historical Sessions
  are never touched, and `ProgramDefinition`'s template graph never
  changes** (CLAUDE.md rule 2 applies to the override mechanism
  exactly as it does to the definition itself).

Both paths validate through `SubstitutionValidator.isValid` first
(slot's `allowedExercises`/`allowedTargets`/`allowedMovementFunctions`/
`allowedModalities`) — an invalid candidate is rejected before either
scope can be chosen, identically.

## 8. Calibration (§12) — no separate database

When the selected exercise (prescribed or substituted) has no usable
history, the engine already returns `.calibrationRequired`
(`DoubleProgressionEngine`'s first branch) rather than guessing a load.
Execution's job is purely presentational:

- Show the prescription (e.g. `8-12 @ 3 RIR`) with **no suggested
  weight card** — replaced by a plain "Calibration set — pick a starting
  weight" prompt, the user's own chosen starting stepper value standing
  in for the missing suggestion.
- The user logs the set exactly as in §3 — weight, reps, actual RIR.
  **This is a completely ordinary `SetResult`, recorded through the
  identical `RecordSetResultUseCase.recordSet` call as every other set**
  — there is no separate "calibration result" type, per the kickoff's
  explicit instruction not to build a disposable calibration store.
- Because it is a normal `SetResult` attached to the exercise's real
  `ExercisePerformanceProfile`, the very next time this exercise is
  prescribed, `hasUsableHistory`/`lastKnownWeight` are already populated
  from it — calibration becomes permanent history the instant it's
  logged, automatically, with no extra step.

## 9. Powerlifting (§13) — same foundation, confirmed

`PowerliftingProgramGenerator`/`StrengthProgressionEngine` already
produce the identical `ExercisePrescription`/`SetPrescription` shape as
Hypertrophy (`StrengthProgressionEngine`'s own doc comment: *"Shared by
every `StrengthProgressionRules`-based family... renamed from
`HypertrophyProgressionEngine` when Stage 4B confirmed it needed no
family-specific logic of its own"*). Main lifts, variations, triples, and
`.linkedToPairedSlot` backoff sets are all just `ExercisePrescription`/
`SetPrescription` rows with different `repRangeLow/High`/`targetWeight`
values already resolved by the generator — **no separate Powerlifting
execution screen is needed**; §1-8 above apply unchanged. The one thing
worth naming: a paired/backoff slot's "Suggested" card (§2) reads its
*own* `ExercisePrescription`'s history exactly like any other exercise —
the pairing relationship is resolved entirely at materialization time
(`StrengthProgressionEngine.resolveWeight`'s `.linkedToPairedSlot` case),
never re-derived live in the execution UI.
